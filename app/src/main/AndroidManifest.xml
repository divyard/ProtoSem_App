<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.divya.business">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_signin"  />
        <activity android:name=".activity_signup" />
        <activity android:name=".activity_first" android:theme="@style/AppThemee"/>
        <activity android:name=".activity_events" android:theme="@style/AppThemee"/>
        <activity android:name=".activity_gie" android:theme="@style/AppThemee"/>
        <activity android:name=".activity_leader" android:theme="@style/AppThemee"/>
        <activity android:name=".activity_tasks" android:theme="@style/AppThemee" />
        <activity android:name=".activity_gallery" android:theme="@style/AppThemee"/>
        <activity android:name=".activity_profile" android:theme="@style/AppThemee"/>
        <activity android:name=".activity_settings" android:theme="@style/AppThemee"></activity>
    </application>

</manifest>